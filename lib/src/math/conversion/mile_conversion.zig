// Mile Conversion

// Centimeters to Miles
pub fn centimetersToMiles(centimeters: f64) f64 {
    return centimeters * 0.000006213712;
}

// Decameters to Miles
pub fn decametersToMiles(decameters: f64) f64 {
    return decameters * 0.006213712;
}

// Decimeters to Miles
pub fn decimetersToMiles(decimeters: f64) f64 {
    return decimeters * 0.00006213712;
}

// Feet to Miles
pub fn feetToMiles(feet: f64) f64 {
    return feet * 0.0001893939;
}

// Hectometers to Miles
pub fn hectometersToMiles(hectometers: f64) f64 {
    return hectometers * 0.06213712;
}

// Inches to Miles
pub fn inchesToMiles(inches: f64) f64 {
    return inches * 0.00001578283;
}

// Kilometers to Miles
pub fn kilometersToMiles(kilometers: f64) f64 {
    return kilometers * 0.6213712;
}

// Meters to Miles
pub fn metersToMiles(meters: f64) f64 {
    return meters * 0.0006213712;
}

// Micrometers to Miles
pub fn micrometersToMiles(micrometers: f64) f64 {
    return micrometers * 0.0000000006213712;
}

// Millimeters to Miles
pub fn millimetersToMiles(millimeters: f64) f64 {
    return millimeters * 0.0000006213712;
}

// Nanometers to Miles
pub fn nanometersToMiles(nanometers: f64) f64 {
    return nanometers * 0.0000000000006213712;
}

// Nautical Miles to Miles
pub fn nauticalMilesToMiles(nauticalMiles: f64) f64 {
    return nauticalMiles * 1.15078;
}

// Picometers to Miles
pub fn picometersToMiles(picometers: f64) f64 {
    return picometers * 0.0000000000000006213712;
}

// Yards to Miles
pub fn yardsToMiles(yards: f64) f64 {
    return yards * 0.0005681818;
}